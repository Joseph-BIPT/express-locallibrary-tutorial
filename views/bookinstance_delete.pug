extends layout

block content

    h1 ID: #{copy_id}

    p Do you really want to delete this book?

        form(method='POST' action='')
            div.form-group
                input#copyid.form-control(type='hidden' name='copyid' required='true' value=copy_id)

            button.btn.btn-primary(type='submit') Delete